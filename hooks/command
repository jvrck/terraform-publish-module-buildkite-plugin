#!/bin/bash

set -xeu

PL=$(cat <<-END
steps:
    - wait
    - command: "echo Deploy!"
        label: ":rocket:"
        agents:
        queue: "default"
END
)

echo -e $PL | buildkite-agent pipeline upload